services:
  - type: web
    name: os-lab-quiz
    env: python
    buildCommand: echo "No build required"
    startCommand: python -m http.server 8000
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
